import styled from "styled-components";

const PageSidebarBodyLabeledRowStyle = styled.div`
	display: flex;
	flex-direction: column;
	gap: 1.5rem;

	& > h3 {
		color: var(--text-1);
	}
`;

export default PageSidebarBodyLabeledRowStyle;

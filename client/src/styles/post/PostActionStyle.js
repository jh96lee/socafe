import styled from "styled-components";

const PostActionStyle = styled.div`
	display: flex;
	align-items: center;
	gap: 0.7rem;

	& > h5 {
		color: var(--text-1);
	}
`;

export default PostActionStyle;

import styled from "styled-components";

const AddContentButtonWrapperStyle = styled.div`
	padding: 1.5rem;
	box-shadow: 0 0px 0 1.6px var(--primary-separator-color);
`;

export default AddContentButtonWrapperStyle;

import styled from "styled-components";

const StatsHeaderStyle = styled.div`
	display: flex;
	justify-content: space-between;
	align-items: center;
	width: 100%;
	padding: 1.6rem 3rem;

	& > h2 {
		color: var(--char-default);
	}
`;

export default StatsHeaderStyle;

import styled from "styled-components";

export const PostContentsStyle = styled.div`
	color: var(--txt-1);
	padding: 0 1rem;

	& p {
		font-size: 1.3rem;
		font-weight: 300;
		letter-spacing: -0.4px;
		line-height: 2.2rem;
	}
`;

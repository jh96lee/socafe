import styled from "styled-components";

export const TextAreaStyle = styled.div`
	color: var(--text-1);

	& p {
		font-size: 1.3rem;
		font-weight: 300;
		letter-spacing: -0.4px;
		line-height: 2.2rem;
	}
`;

import styled from "styled-components";

export const StatsGraphStyle = styled.div`
	grid-column: 2 / 3;
	grid-row: 1 / 2;
	position: relative;
	border-bottom: 1px solid var(--graph-border-default);
	border-left: 1px solid var(--graph-border-default);
`;

import styled from "styled-components";

export const HomeFeedStoryUserStyle = styled.div`
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 0.7rem;

	& > p {
		color: var(--char-default);
		font-size: 1.3rem;
	}
`;

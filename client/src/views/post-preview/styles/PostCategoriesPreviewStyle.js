import styled from "styled-components";

export const PostCategoriesPreviewStyle = styled.div`
	display: flex;
	align-items: center;
	gap: 0.7rem;
`;

export const PostCategoryPreviewStyle = styled.p`
	font-size: 1.37rem;
	color: var(--primary-text-color);
	background-color: var(--tertiary-background-color);
	padding: 0.7rem 1.4rem;
	border-radius: 2rem;
`;

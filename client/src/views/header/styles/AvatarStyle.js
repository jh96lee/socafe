import styled from "styled-components";

const AvatarStyle = styled.div`
	& > img {
		display: block;
		width: 3.3rem;
		height: 3.3rem;
		border-radius: 50%;
		cursor: pointer;
	}
`;

export default AvatarStyle;

import styled from "styled-components";

export const RegisterStepStyle = styled.div`
	display: flex;
	flex-direction: column;
	gap: 0 1rem;
	color: var(--primary-text-color);
	margin-bottom: 2.2rem;

	& span {
		font-size: 1.43rem;
	}
`;

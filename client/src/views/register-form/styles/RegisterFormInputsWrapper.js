import styled from "styled-components";

const RegisterFormInputsWrapper = styled.div`
	margin: 1.8rem 0 2.9rem 0;

	& div:not(:last-child) {
		margin-bottom: 1rem;
	}
`;

export default RegisterFormInputsWrapper;

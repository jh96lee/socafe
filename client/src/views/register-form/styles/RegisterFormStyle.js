import styled from "styled-components";

import { FormStyle } from "../../../styles";

const RegisterFormStyle = styled(FormStyle)`
	& a {
		color: var(--primary-text-color);
	}
`;

export default RegisterFormStyle;

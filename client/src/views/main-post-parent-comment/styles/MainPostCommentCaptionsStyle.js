import styled from "styled-components";

export const MainPostCommentCaptionsStyle = styled.div`
	margin: 0.4rem 0 0.6rem 0;

	& a {
		color: var(--text-1);
		font-weight: 500;
	}

	& span {
		color: var(--text-1);
	}
`;
